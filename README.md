OpenPNE JMeter Scenarios
========================

Usage
-----

Execute following command (see bin/test\_sample.sh)

    ant all -Djmeter.home=/path/to/jmeter -Dopenpne_sns_host=localhost -Dopenpne_port=80

then, results/ directory will be created.

